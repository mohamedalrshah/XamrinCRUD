<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamrinFirstApp.Views.CountryUpdate">
    <ContentPage.Content>
        <StackLayout VerticalOptions="StartAndExpand" HorizontalOptions="FillAndExpand">
            <Label Text="Welcome to Xamarin.Forms!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />

            <Label x:Name="Idlabel" Text="{Binding currentCountry.Id}"/>
            <Entry x:Name="NameBox" Text="{Binding currentCountry.Name}"/>
            <Entry x:Name="ShortNameBox" Text="{Binding currentCountry.ShortName}"/>
            <Label x:Name="DTlabel" Text="{Binding currentCountry.CreationDateTime}"/>
            <Button x:Name="SaveButton" Text="Update Country" Clicked="Button_Clicked"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>